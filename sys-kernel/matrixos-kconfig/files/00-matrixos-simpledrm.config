# Enable SIMPLE FB and DRM to avoid bundling nvidia/amdgpu firmwares
# for early KMS support, reducing initramfs size dramatically.
# Once the rootfs is mounted, the kernel will load the drivers from
# it.
CONFIG_DRM_SIMPLEDRM=y
CONFIG_SYSFB_SIMPLEFB=y
